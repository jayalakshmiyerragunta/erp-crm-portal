# Base URL for the backend API. Leave empty for local dev (Vite dev proxy will forward /api to localhost:5000).
# For production (e.g. Vercel frontend + Railway backend), set this to your deployed backend URL.
# Example: VITE_API_URL=https://your-backend.up.railway.app/api/v1
VITE_API_URL=